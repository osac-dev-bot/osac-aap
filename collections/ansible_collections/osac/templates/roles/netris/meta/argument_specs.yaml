---
argument_specs:
  create_virtual_network:
    options:
      virtual_network:
        type: dict
        required: true
        description: VirtualNetwork CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_virtual_network:
    options:
      virtual_network:
        type: dict
        required: true
        description: VirtualNetwork CR from fulfillment-api via EDA payload

  create_subnet:
    options:
      subnet:
        type: dict
        required: true
        description: Subnet CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_subnet:
    options:
      subnet:
        type: dict
        required: true
        description: Subnet CR from fulfillment-api via EDA payload

  create_security_group:
    options:
      security_group:
        type: dict
        required: true
        description: SecurityGroup CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_security_group:
    options:
      security_group:
        type: dict
        required: true
        description: SecurityGroup CR from fulfillment-api via EDA payload

  create_public_ip_pool:
    options:
      public_ip_pool:
        type: dict
        required: true
        description: PublicIPPool CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_public_ip_pool:
    options:
      public_ip_pool:
        type: dict
        required: true
        description: PublicIPPool CR from fulfillment-api via EDA payload

  create_public_ip:
    options:
      public_ip:
        type: dict
        required: true
        description: PublicIP CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_public_ip:
    options:
      public_ip:
        type: dict
        required: true
        description: PublicIP CR from fulfillment-api via EDA payload

  create_external_ip:
    options:
      external_ip:
        type: dict
        required: true
        description: ExternalIP CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_external_ip:
    options:
      external_ip:
        type: dict
        required: true
        description: ExternalIP CR from fulfillment-api via EDA payload

  create_nat_gateway:
    options:
      nat_gateway:
        type: dict
        required: true
        description: NATGateway CR from fulfillment-api via EDA payload
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_nat_gateway:
    options:
      nat_gateway:
        type: dict
        required: true
        description: NATGateway CR from fulfillment-api via EDA payload

  create_network_attachment:
    options:
      network_attachment:
        type: dict
        required: true
        description: >-
          Network attachment parameters from dispatcher. Contains host_id
          (Netris server name), interface (physical port name), subnet_ref
          (OSAC Subnet UUID), security_group_refs, and primary flag.
      template_parameters:
        type: dict
        description: Template-specific parameters (reserved for future use)
        options: {}
        default: {}

  delete_network_attachment:
    options:
      network_attachment:
        type: dict
        required: true
        description: >-
          Network attachment parameters from dispatcher. Contains host_id
          (Netris server name), interface (physical port name), and subnet_ref
          (OSAC Subnet UUID).
